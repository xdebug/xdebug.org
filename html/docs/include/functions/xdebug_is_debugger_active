= xdebug_is_debugger_active,2.6
Returns whether a debugging session is active
bool
none
FUNC_REMOTE

TXT:
Returns <code>true</code> if a debugging session through DBGp is currently
active with a client attached; <code>false</code>, if not.
